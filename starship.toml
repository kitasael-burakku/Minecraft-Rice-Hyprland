"$schema" = 'https://starship.rs/config-schema.json'
# Generado por matugen — no editar a mano.
# Estático de referencia: ~/.config/starship.static.toml

# Se agregó el inicio del bloque derecho [] directamente en el formato principal
format = """
$status\
$os\
${custom.user_pretty}\
${custom.hostname_pretty}\
$directory\
$fill\
[](fg:#261e19)\
$git_branch\
$git_status\
$python\
$nodejs\
$rust\
$golang\
${custom.right_cap}\
$line_break\
$cmd_duration\
$character\
"""

command_timeout = 500
add_newline = false

# ── Fill (Relleno central limpio) ────────────────────────────────
[fill]
symbol = " "

# ── OS (Inicio del bloque izquierdo) ────────────────────────────
[os]
disabled = false
style = "bg:#221a15 fg:#ffb688"
format = "[](fg:#221a15)[ $symbol ]($style)"

[os.symbols]
Arch        = "󰣇"
CachyOS     = "󰣇"
Manjaro     = ""
Fedora      = ""
Ubuntu      = ""
Debian      = ""
NixOS       = ""
EndeavourOS = ""
Garuda      = "󰛓"
openSUSE    = ""
Gentoo      = "󰣨"
Alpine      = ""
Unknown     = "󰌽"

# ── Usuario ─────────────────────────────────────────────────────
[custom.user_pretty]
disabled = false
command = "echo $USER_PRETTY"
when = "test -n \"$USER_PRETTY\""
style = "bg:#261e19 fg:#ffb688 bold"
format = "[](bg:#261e19 fg:#221a15)[ $output ]($style)"

# ── Hostname ────────────────────────────────────────────────────
[custom.hostname_pretty]
command = "hostnamectl --pretty 2>/dev/null || hostname"
when = "true"
style = "bg:#261e19 fg:#9f8d83"
format = "[](bg:#261e19 fg:#261e19)[ 󰍹 $output ]($style)"

# ── Directorio ──────────────────────────────────────────────────
[directory]
style = "bg:#261e19 fg:#caca93 bold"
format = "[](bg:#261e19 fg:#261e19)[ $path ]($style)[](fg:#261e19 bg:none) "
home_symbol = "󱂵 ホーム"
read_only = "  読取専用"
truncation_length = 3
truncation_symbol = "…/"

[directory.substitutions]
"Desktop"   = "󰇄 デスクトップ"
"Documents" = "󰈙 ドキュメント"
"Downloads" = "󰇚 ダウンロード"
"Music"     = "󰎈 音楽"
"Pictures"  = "󰏏 写真"
"Videos"    = "󰕧 ビデオ"
"Projects"  = "󱌢 プロジェクト"
"GitHub"    = "󰊤 ギットハブ"
"Code"      = "󰨞 コード"
".config"   = "󰒓 設定"

# ── Git branch ──────────────────────────────────────────────────
[git_branch]
symbol = "󰊢"
style = "bg:#261e19 fg:#d7c3b8 bold"
truncation_length = 18
# Se elimina el separador manual aquí; el formato principal ya maneja la apertura
format = "[ $symbol $branch ]($style)"

# ── Git status ──────────────────────────────────────────────────
[git_status]
style = "bg:#261e19 fg:#ffb4ab bold"
format = "[$all_status$ahead_behind ]($style)"
conflicted = "󰞇 "
ahead      = "⇡${count}"
behind     = "⇣${count}"
diverged   = "⇕"
untracked  = "󰋗 "
stashed    = "󰆼"
modified   = "󰏫 "
staged     = "󰐗 "
renamed    = "󰕚 "
deleted    = "󰍶 "

# ── Lenguajes (cada uno a un rol distinto para conservar variedad) ──────
[python]
symbol = ""
style = "bg:#261e19 fg:#e6e6ad bold"
format = "[$symbol $version ]($style)"

[nodejs]
symbol = ""
style = "bg:#261e19 fg:#ffdbc7 bold"
format = "[$symbol $version ]($style)"

[rust]
symbol = ""
style = "bg:#261e19 fg:#e5bfa8 bold"
format = "[$symbol $version ]($style)"

[golang]
symbol = ""
style = "bg:#261e19 fg:#ffdbc7 bold"
format = "[$symbol $version ]($style)"

# ── Cierre bloque derecho ────────────────────────────────────────
[custom.right_cap]
command = "echo"
when = "true"
# Cierra el bloque de lenguajes/git con un borde redondeado simétrico
format = "[](fg:#261e19 bg:none)"

# ── Duración del comando ─────────────────────────────────────────
[cmd_duration]
min_time = 2000
show_milliseconds = false
style = "fg:#f0dfd7 italic"
format = "[ 󰔚 $duration ]($style)"

# ── Prompt character ─────────────────────────────────────────────
[character]
success_symbol = "[ 󰁔](fg:#ffb688 bold)"
error_symbol   = "[ 󰁔](fg:#ffb4ab bold)"

# ── Status (Error previo) ────────────────────────────────────────
[status]
disabled = false
style = "fg:#ffb4ab bold"
format = "[󰅚 $status ]($style)"

# ── Módulos deshabilitados ───────────────────────────────────────
[package]
disabled = true
[memory_usage]
disabled = true
[time]
disabled = true
[line_break]
disabled = false
